'use strict';

describe('Template.customerAudit', function () {

	// beforeEach(function () {
  //   MeteorStubs.install();
  // });
	//
  // afterEach(function () {
  //   MeteorStubs.uninstall();
  // });

	describe('helpers', function () {

		describe('userEmail', function () {

			beforeEach(function () {
				// mock(window, 'Meteor');
			});

			it('should return an email address for a user ID', function () {
				// console.log(Meteor);
				// spyOn(Meteor, 'find').and.returnValue(result);


			});

		});

	});
});
